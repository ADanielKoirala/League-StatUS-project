# League Stat-Us Frontend

React frontend for the League Stat-Us volleyball league management platform.

## Run locally

```bash
npm install
npm start
```

The app expects the Django API to be running locally. Check `src/api.js` for the API base URL.
